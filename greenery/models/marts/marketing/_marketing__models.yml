version: 2

models:
  - name: fact_page_views
    description: All page view events from greenery’s events data
    columns:
      - name: session_length_minutes
        description: length of a single session
        tests:
          - positive_values